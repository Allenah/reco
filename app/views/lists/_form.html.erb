<%= simple_form_for(@list) do |f| %>
    <%= f.input :list_type, :label => "Type", collection: ['restaurants', 'movies', 'albums'], as: :select, prompt: 'Select list' %>
    <%= f.input :name, :label => "Title" %>
    <%= f.input :photo %>
    <%= f.input :photo_cache, as: :hidden %>
    <%= f.association :tags, collection: Tag.all %>
    <%= f.input :description, input_html: {maxlength: 200} %>
    <%= f.submit "Add list", class: "btn btn-primary" %>
<% end %>


